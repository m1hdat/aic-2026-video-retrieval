milvus:
  mode: lite
  uri: data/milvus/aic2026_milvus.db
  collection_name: aic2026_keyframes
  metric_type: IP

model:
  name: openai/clip-vit-base-patch32
  embedding_dim: 512

data:
  manifest_keyframes: data/processed/manifest_keyframes.parquet
  manifest_videos: data/processed/manifest_videos.parquet

app:
  use_mock: false
  top_k: 100